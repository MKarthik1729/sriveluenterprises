const data = Array.from({ length: 32 }, (_, i) => ({
    // id: i + 1,
    src: `/posts/images${i + 1}.jpeg`,
    width: 180,
    height: 180,
    // caption: "After Rain (Jeshu John - designerspics.com)",
  }));

  export default data